# 明确需求设计文档（后端）

## 0. 文档版本
- 当前版本：`v<必填>`
- 文档状态：`草稿/评审中/已冻结`

### 0.1 版本规则（固定）
- 来源版本：问题 `Qx` 首次进入台账时的文档版本。
- 决议版本：问题 `Qx` 被确认闭环时的文档版本。
- 版本递增：每次业务含义变更后升一个小版本（如 `v1.0 -> v1.1`）。

### 0.2 版本迭代记录
| 版本 | 变更摘要 | 解决Q项 | 未解决Q项 |
|---|---|---|---|
| v1.0 | `<首次建立>` | `<Qx...>` | `<Qx...>` |

### 0.3 编号与ID定义规则（固定）
| 标识 | 规则 | 示例 |
|---|---|---|
| `F` 功能编号 | `F` + 递增数字，不复用 | `F1,F2` |
| `C` 契约编号 | `C` + 递增数字，不复用 | `C1,C2` |
| `Q` 闭环问题ID | `Q` + 递增数字，不复用、不重排 | `Q1,Q2` |
| `R` 复评风险编号 | `R` + 递增数字，仅用于 `final-check` 非阻塞风险输出，不回填正文 | `R1,R2` |
| `D` 默认决策编号 | `D` + 递增数字，不复用 | `D1,D2` |
| `A` 验收编号 | `A` + 递增数字，不复用 | `A1,A2` |
| `U` 待编目问题ID | `U` + 递增数字，仅用于临时问题输入 | `U1,U2` |

## 1. 需求背景与目标
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
- 业务背景：`<为什么要做>`
- 核心目标：`<要达成什么结果>`
- 业务价值：`<效率/质量/合规/成本>`

## 2. 范围定义（In/Out Scope）
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
- In Scope：
  - `<功能点1>`
  - `<功能点2>`
- Out Scope：
  - `<不做项1>`
  - `<不做项2>`
- 上下游边界：`<涉及系统/角色边界>`

## 3. 术语定义
> 术语分域规则统一遵循 `CL-16`（见 `references/close-loop-checklist.md`）。
### 3.1 领域术语
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`

| 术语 | 定义 |
|---|---|
| `<术语>` | `<定义>` |

### 3.2 业务状态术语（仅业务）
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
> 仅填写“业务流程状态”。若无业务状态机，此表可留空。
> 禁止用页面交互（点击、跳转、弹窗开关）替代业务状态定义。

| 状态 | 含义 |
|---|---|
| `<状态>` | `<说明>` |

## 4. 角色与权限需求
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`

| 角色 | 可查看 | 可操作 | 数据范围 |
|---|---|---|---|
| `<角色>` | `<是/否>` | `<动作列表>` | `<范围>` |

## 5. 功能需求清单
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`

| 编号 | 功能名称 | 输入 | 输出 | 成功标准 |
|---|---|---|---|---|
| F1 | `<名称>` | `<输入>` | `<输出>` | `<标准>` |

## 6. 业务规则（闭环）
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
> `生命周期与状态流转` 仅描述业务对象状态机（状态、迁移条件、失败路径、回滚），不描述页面交互路径。
- 唯一性与标识：`<规则>`
- 生命周期与状态流转：`<规则>`
- 异常与失败处理：`<规则>`
- 并发与一致性：`<规则>`
- 依赖与影响：`<规则>`
- 默认值与边界上限：`<规则>`

## 7. 业务对象与逻辑数据结构
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
> 要求：
> - [T7-01] 按对象分表，不要把多个对象字段放在同一个总表；
> - [T7-02] 本章仅描述“单一独立最小业务对象 + 对象关系/聚合关系（可选）”；
> - [T7-03] 不得出现“输入对象/输出对象/请求对象/响应对象/草稿对象/编辑对象”等操作I/O命名；
> - [T7-04] 对象名与字段名优先使用输入中的中文业务名称定义。
> - [T7-05] 若字段属于文本自由输入，其约束统一遵循 `CL-07` 与 `CL-05`；至少补齐 `必填性 + 长度（最小/最大及计数口径）`，若本轮无法明确则标记 `TODO`。

### 7.1 基础业务对象（最小单元）
#### 7.1.1 `<对象名称（中文）>`
| 字段 | 必填 | 约束 | 说明 |
|---|---|---|---|
| `<字段（中文）>` | `<是/否>` | `<规则>` | `<说明>` |

#### 7.1.2 `<对象名称（中文）>`
| 字段 | 必填 | 约束 | 说明 |
|---|---|---|---|
| `<字段（中文）>` | `<是/否>` | `<规则>` | `<说明>` |

### 7.2 关系对象（关联实体）
#### 7.2.1 `<关系对象名称（中文）>`
| 字段 | 必填 | 约束 | 说明 |
|---|---|---|---|
| `<字段（中文）>` | `<是/否>` | `<规则>` | `<说明>` |

### 7.3 聚合对象（可选，仅场景视图）
> 仅在页面/场景确有聚合展示语义时填写；不得替代基础对象定义。

#### 7.3.1 `<聚合对象名称（中文）>`
| 字段 | 必填 | 约束 | 说明 |
|---|---|---|---|
| `<字段（中文）>` | `<是/否>` | `<规则>` | `<说明>` |

### 7.4 对象关系约束
| 关系 | 约束说明 |
|---|---|
| `<对象A -> 对象B>` | `<1:1 / 1:N / N:M + 约束>` |

## 8. 逻辑接口契约（技术无关）
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
> 要求：
> - [T8-01] 本章专门承载操作级输入/输出；
> - [T8-02] 输入/输出以原型语义为主（中文业务字段），避免以英文技术字段主导表达；
> - [T8-03] 每条契约的关键输入/输出字段应可追溯到第7章对象定义。
> - [T8-04] 本章仅保留 `编号/场景/输入/输出/失败条件` 五列，不设置“评审意见/决议”列。
> - [T8-05] 失败条件中的待判定表述、文本自由输入校验限制统一按 `CL-05/CL-07` 补齐到正文规则与验收标准；若本轮无法明确，标记 `TODO`。

| 编号 | 场景 | 输入 | 输出 | 失败条件 |
|---|---|---|---|---|
| C1 | `<场景>` | `<输入（中文业务语义）>` | `<输出（中文业务语义）>` | `<失败>` |

## 9. 非功能需求
清晰度状态：`清晰/不清晰/逻辑未闭环/规则模糊`
- 安全与鉴权：`<要求>`
- 可追溯性：`<要求>`
- 一致性：`<要求>`
- 可扩展性：`<要求>`

## 10. 闭环治理问题台账（必填）
> 仅使用 `Q1,Q2...` 编号。编号一经创建不改、不复用。

### 10.1 用户新增问题输入区（待编目）
> 用途：由用户直接补充新问题或建议。
> `U/Q` 编目与状态合法性统一遵循 `references/close-loop-checklist.md` 第7节。
> `关联C编码` 为可选，仅当问题指向第8章某条契约时填写。
> `处理结论` 取值统一遵循第7节：`已编目->Qx / 不编目（已澄清） / 待处理`。

| 临时问题ID | 问题描述 | 关联C编码（可选） | 建议优先级 | 编目状态 | 处理结论 |
|---|---|---|---|---|---|
| U1 | `<用户补充的问题>` | `<C1/留空>` | `<高/中/低>` | `<待处理/已编目/不编目>` | `<已编目->Qx/不编目（已澄清）/待处理>` |

### 10.2 台账治理状态定义（非业务术语）
> 状态判定与回填证据口径统一遵循 `references/close-loop-checklist.md` 第7节。

| 状态 | 定义 |
|---|---|
| `OPEN` | 见 `references/close-loop-checklist.md` 第7节。 |
| `RESOLVED` | 见 `references/close-loop-checklist.md` 第7节。 |
| `DEFERRED` | 见 `references/close-loop-checklist.md` 第7节。 |

### 10.3 Q台账
> 规则：`Q` 编号递增且不重排。
> 字段必填性、状态合法性、回填证据、模型问题入账统一遵循 `references/close-loop-checklist.md` 第7节。

| 问题ID | 分类 | 对应检查项(CL-xx) | 问题描述 | 问题来源（用户/模型） | 影响范围 | 证据引用 | 来源版本 | 目标版本 | 当前状态(OPEN/RESOLVED/DEFERRED) | 回复内容 | 决议版本 | 落地位置 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Q1 | `<分类>` | `<CL-xx>` | `<问题>` | `<用户/模型>` | `<范围>` | `<章节/表格/条目>` | `v1.0` | `<v1.1>` | `OPEN` | `<回复>` | `<版本或->` | `<章节/规则编号>` |

## 11. 默认决策清单（可选）
| 编号 | 默认决策 | 依据 | 风险 | 回滚条件 | 用户回复（认同/不认同/修改） | 回填结果 |
|---|---|---|---|---|---|---|
| D1 | `<决策>` | `<依据>` | `<风险>` | `<条件>` | `<待回复/认同/不认同/修改建议>` | `<保留/调整后内容/TODO>` |

## 12. 闭环完成度检查（必填）
| 检查项 | 状态（通过/缺失） | 说明 | 阻塞级别（阻塞/非阻塞） |
|---|---|---|---|
| 范围是否清晰 | `<状态>` | `<说明>` | `<级别>` |
| 功能是否可验证 | `<状态>` | `<说明>` | `<级别>` |
| 状态机是否闭环 | `<状态>` | `<说明>` | `<级别>` |
| 关键规则是否可判定 | `<状态>` | `<说明>` | `<级别>` |
| 输入输出契约是否完整 | `<状态>` | `<说明>` | `<级别>` |
| 权限与数据边界是否明确 | `<状态>` | `<说明>` | `<级别>` |
| 异常策略是否明确 | `<状态>` | `<说明>` | `<级别>` |

## 13. 验收标准
| 编号 | 验收项 | 验收方式 | 通过标准 |
|---|---|---|---|
| A1 | `<验收项>` | `<评审/测试/演示>` | `<标准>` |

## 14. 闭环结论
- 当前结论：`闭环完成 / 未闭环`
- 阻塞Q项：`<Qx...>`
- 下一步动作：`<动作>`
